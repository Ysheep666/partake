box: wercker/nodejs
services:
  - wercker/redis@1.0.1
  - wercker/mongodb@1.0.1
script: |
  sudo npm install gulp -g
  sudo npm install bower -g
build:
  steps:
    - bower-install
    - npm-install
    - npm-test
