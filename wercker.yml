box: wercker/nodejs@1.4.0
services:
  - wercker/redis@1.0.1
  - wercker/mongodb@1.0.1
script: |
  sudo npm install gulp -g
  sudo npm install bower -g
build:
  steps:
    - npm-install
    - bower-install
    - npm-test
